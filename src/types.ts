export type Theme = {
  dark: {
    bg: string;
    primaryText: string;
    muted: string;
    bgButton: string;
    bgPanel: string;
  }
  light: {
    bg: string;
    primaryText: string;
    muted: string;
    bgButton: string;
    bgPanel: string;
  }
}